// function setup() {
//   // setup the page
//   let canvas = createCanvas(windowWidth * 0.4, windowWidth * 0.4); // create a canvas
//   canvas.parent("canvas2"); // attach the canvas to the container
// }

// function draw() {
//   // draw the page
//   background(140); // set the background to black
//   fill(255); // set the fill to white
//   // text("Hello World!", 10, 10); // write text to the canvas
//   let x = map(mouseX, 0, windowWidth, 0, width); // map the x position of the mouse to a value between 0 and 255
//   let y = map(mouseY, 0, windowHeight, 0, height); // map the y position of the mouse to a value between 0 and 255
//   ellipse(x, y, 50, 50); // draw an ellipse at the mouse position
// }
